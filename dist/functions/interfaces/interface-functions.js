"use strict";
let firstSum = (num1, num2) => {
    return num1 + num2;
};
console.log(firstSum(2, 5));
const calculator = {
    sum: (a, b) => a + b,
    sub: (a, b) => a - b,
    multiply: (a, b) => a * b,
    division: (a, b) => a / b,
};
console.log(calculator);
